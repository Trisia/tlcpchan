services:
  tlcpchan:
    build: .
    container_name: tlcpchan
    restart: unless-stopped
    ports:
      - "30080:30080"
      - "30000:30000"
      - "443:443"
    volumes:
      - ./config:/app/config:ro
      - ./certs:/app/certs:ro
      - ./logs:/app/logs
    environment:
      - TZ=Asia/Shanghai
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:30080/api/v1/system/health"]
      interval: 30s
      timeout: 10s
      retries: 3
